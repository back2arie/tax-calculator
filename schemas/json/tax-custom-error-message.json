[
    {
        "id": "tax_custom_error_messages",
        "description": "custom error messages from sales",
        "properties": {
            "salesIds": [{
                "contains": "does not match",
                "replaceWith": "value for salesId should contains numeric"
            }],
            "salesGroupId": [{
                "contains": "does not match",
                "replaceWith": "value for salesGroupId should contains numeric"
            }]
        }
    },
    {
        "id": "tax_get_all_error_messages",
        "description": "custom error messages for get all sales",
        "properties": {
            "limit": [{
                "contains": "does not match",
                "replaceWith": "value for limit should contains numeric"
            }],
            "page": [{
                "contains": "does not match",
                "replaceWith": "value for page should contains numeric"
            }],
            "orderBy": [{
                "contains": "does not match",
                "replaceWith": "value for orderBy should contains numeric and alphabet"
            }],
            "sort": [{
                "contains": "does not match",
                "replaceWith": "value for sort should contains asc or desc"
            }]
        }
    },
    {
        "id": "sales_active_deactive_custom_error_messages",
        "description": "custom error messages for active deactive sales",
        "properties": {
            "salesId": [{
                "contains": "does not match",
                "replaceWith": "value for salesId should contains numeric"
            }],
            "status": [{
                "contains": "does not match",
                "replaceWith": "value for status should contains alphabet"
            }]
        }
    }
]
